<resources>

    <!-- Base application theme. -->
    <style name="NavDrawerAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="NavDrawerAppTheme.FullScreen" parent="NavDrawerAppTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="AppTheme.FullScreen.WhiteHamburgerIcon" parent="NavDrawerAppTheme.FullScreen">
        <item name="drawerArrowStyle">@style/DrawerArrowStyleWhite</item>
    </style>

    <style name="AppTheme.FullScreen.BlackHamburgerIcon" parent="NavDrawerAppTheme.FullScreen">
        <item name="drawerArrowStyle">@style/DrawerArrowStyleBlack</item>
    </style>

    <style name="DrawerArrowStyleWhite" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>
    <style name="DrawerArrowStyleBlack" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/black</item>
    </style>

</resources>
